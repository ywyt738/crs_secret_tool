language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
before_deploy:
  # Set up git user name and tag this commit
  - git config --local user.name "HuangXiaojun"
  - git config --local user.email "120968816@qq.com"
  - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"
script:
  - pyinstaller -F crs_secret_tool.py
deploy:
  provider: releases
  api_key:
    secure: 5dd03cac13e48a7b47ad47a2fd4c1447ebad19ea
  file: dist/crs_secret_tool
  skip_cleanup: true